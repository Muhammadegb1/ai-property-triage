# n8n environment variables (Settings → Variables)

# --- n8n in Docker Desktop (Windows/Mac) — services on host ---
GUARDRAILS_URL=http://host.docker.internal:8001
RAG_URL=http://host.docker.internal:8002
IMAGE_URL=http://host.docker.internal:8003
LANGGRAPH_URL=http://host.docker.internal:8004

# --- n8n.cloud OR n8n on host (not in Docker) — use localhost or EC2 public IP ---
# GUARDRAILS_URL=http://127.0.0.1:8001
# RAG_URL=http://127.0.0.1:8002
# IMAGE_URL=http://127.0.0.1:8003
# LANGGRAPH_URL=http://127.0.0.1:8004

# --- EC2 production (partner) ---
# GUARDRAILS_URL=http://EC2_PUBLIC_IP:8001
# ...

# Node 8 — team routing (optional)
RESIDENTIAL_TEAM_WEBHOOK_URL=
COMMERCIAL_TEAM_WEBHOOK_URL=

# WebUI (.env) — n8n Docker local examples:
# Production: N8N_WEBHOOK_URL=http://localhost:5678/webhook/property-triage
# Test (one call): .../webhook-test/property-triage
